QSTASH_TOKEN=

# Authentication (required when AUTH_DISABLED is not true)
# Generate with: openssl rand -base64 48
JWT_SECRET=

# Set to "true" to disable authentication (for local development)
AUTH_DISABLED=

# API key for evaluation harness (separate from ANTHROPIC_API_KEY to avoid
# accidental charges when using Claude Code with a different billing setup)
ANTHROPIC_EVAL_API_KEY=
