[project]
name = "fastmcp-evaluation"
version = "0.1.0"
description = "Evaluation harness for FastMCP servers using Claude"
requires-python = ">=3.11"
dependencies = [
    "anthropic>=0.75.0",
    "fastmcp>=2.0.0",
    "python-dotenv>=1.0.0",
]
